import { describe, it, expect } from 'vitest';
import { BiddingPanel } from '../ui/components/BiddingPanel';

describe('BiddingPanel', () => {
  it('renders without crash', () => {
    expect(BiddingPanel).toBeDefined();
  });
});


